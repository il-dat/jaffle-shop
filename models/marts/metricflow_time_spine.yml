models:
  - name: metricflow_time_spine
    description: Time spine table for MetricFlow, one row per day.
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        granularity: day
